class Solution {
public:
    int majorityElement(vector<int>& nums) {
        // int count = 0;
        // int candidate = 0;

        // for (int i=0;i<nums.size();i++) {
        //     if (count == 0) {
        //         candidate = nums[i];
        //         count =1;
        //     }
        //     else if (nums[i] == candidate) {
        //         count++;
        //     } 
        //     else {
        //         count--;          ///ek lements ko dusre elements se katate jayenge or jo last m bacehnga wahi hoga 
        //     }
        // }
        // return candidate;    // y trick bas max two unique eelemnets k liye hi  kaam kregi
        
        
        // int n=nums.size();
        // // int a=n/2;
        // for ( int i=0;i<n;i++){
        //     int count =0;
        //     for ( int j=0; j<n;j++){
        //         if ( nums[i]==nums[j]){
        //             count ++;
        //         }
        //     }
        //     if ( count >n/2){
        //         return nums[i];
        //     }
        // }
        // return -1;



        map < int,int > mp;
        for ( int i=0;i<nums.size();i++){
            mp[nums[i]]++;
        }
        for ( auto  it :mp){
            if ( it.second >(nums.size()/2)){
                return it.first ;
            }
        }
        return -1;
    }
};
