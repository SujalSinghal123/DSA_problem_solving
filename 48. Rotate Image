class Solution {
public:
    void rotate(vector<vector<int>>& matrix) {
        int n=matrix.size();
        for ( int i=0;i<=n-2;i++){////////////////////transpose (swapo) phir reverse 
             for ( int j=i+1;j<=n-1;j++){
                swap ( matrix[i][j],matrix[j][i]);
            }
        }
        for ( int i=0;i<n;i++){
        reverse (matrix[i].begin(),matrix [i].end());
        }

///////////brute m matrix[j][n-1-i]=matrix[i][j]; yhi krna h ek nya array bnake usme put krr dnege matrix [n][m] me ........
    }
};
