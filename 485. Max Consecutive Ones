class Solution {
public:
    int findMaxConsecutiveOnes(vector<int>& nums) {
        int count =0;
        // int y=INT_MIN;
        int ans =0;
        int n=nums.size();
        for ( int i=0;i<nums.size();i++)
        {
            // if (nums[i]==0){
            //     return 0;
            // }
            if (nums[i]==1){
                // return n;
                count++;
                // ans=max(ans,count); /////y line bhi likh skte h 
            }
            // ans =count ;
            else {
                count=0;
                // ans=count;
            }
                ans=max(ans,count);


        }
        // if ( count >)

        // int result  =max(y,ans );
        return ans ;
    }
};
